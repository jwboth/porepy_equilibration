services:
  example1:
    build: .
    # Mount the host's ./output directory into the container so that
    # any output files produced by the example runs are immediately available
    # on the host after the container exits.
    volumes:
      - ./output:/work/output
