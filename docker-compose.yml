services:
  example1:
    build: .
    # Mount the host's ./visualization directory into the container so that
    # any output files produced by apps.example1 are immediately available on
    # the host after the run completes.
    volumes:
      - ./visualization:/work/visualization
